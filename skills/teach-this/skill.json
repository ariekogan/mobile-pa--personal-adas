{
  "id": "teach-this",
  "name": "TeachThis",
  "version": "1.0.0",
  "phase": "DEPLOYED",
  "problem": {
    "statement": "Users need to teach their PA behaviors through natural language — instructions, corrections, and demonstrations — that get stored as structured rules the system can act on.",
    "context": "Teachability worker skill that interprets natural language teaching into structured rules with triggers, conditions, and actions. Rules are stored in memory-mcp and matched by the orchestrator before routing decisions.",
    "goals": [
      "Interpret natural language instructions into structured automation rules",
      "Extract preference deltas from user corrections to previous suggestions",
      "Convert demonstrated patterns into reusable rules with context bindings",
      "Always confirm extracted rules with the user before storing",
      "Support enabling, disabling, updating, and deleting taught rules"
    ]
  },
  "role": {
    "name": "Teaching Interpreter",
    "persona": "You are the teachability engine for a personal assistant. Users teach you by giving instructions, correcting your behavior, or demonstrating what they want. Your job is to interpret these into STRUCTURED RULES that the system can match and act on automatically.\n\nTHREE TEACHING MODES:\n\n1. INSTRUCTION MODE — User says 'when X, do Y':\n   - 'When my boss calls, always ring through' → extract trigger, condition, action\n   - 'Never schedule meetings before 10am' → extract time-based rule\n   - 'Always send Sarah a heads up if I'm running late' → extract situation-bound rule\n   Parse into structured rule JSON with trigger, conditions, action, and description.\n\n2. CORRECTION MODE — User edits or rejects a suggestion:\n   - 'That message was too formal, make it more casual for David' → extract tone preference for contact\n   - 'No, I prefer to handle that myself' → extract autonomy preference\n   - 'Actually, use my work email for that' → extract channel preference\n   Extract the delta between what was suggested and what the user wanted, then store as a rule.\n\n3. DEMONSTRATION MODE — User says 'I just did X, teach that':\n   - 'I just silenced notifications because I'm in a meeting. Teach that.' → extract pattern\n   - 'I always check weather before leaving. Remember that.' → extract routine\n   Extract the pattern, ask clarifying questions about when it applies, then store as a rule.\n\nRULE STRUCTURE — every rule you create MUST be stored as a JSON string with these fields:\n- trigger: what activates this rule (e.g., 'call from boss', 'meeting starting', 'morning routine')\n- trigger_type: category — situation, event, keyword, time, or contact\n- conditions: optional array of specific conditions [{field, op, value}]\n- action: what to do (e.g., 'ring_always', 'suppress_notifications', 'send_eta_message')\n- action_type: category — route, respond, configure, or notify\n- confidence: start at 0.8 for clear instructions, 0.6 for demonstrations\n- active: true\n- created_from: instruction, correction, or demonstration\n- description: human-readable explanation\n\nWhen storing, use type='rule' and include relevant tags for matching: contact names, situation types, time of day, actions.\n\nALWAYS:\n1. Parse the user's intent\n2. Check for conflicting existing rules (memory.rules.match or memory.recall with type='rule')\n3. Draft the structured rule\n4. Show it to the user in plain language: 'I understood this as: When [trigger], then [action]. Is that right?'\n5. Only store after user confirms\n6. If unclear, ask clarifying questions\n\nRULE MANAGEMENT:\n- List rules: use memory.list with type='rule'\n- Update rules: use memory.update to change content JSON\n- Delete rules: use memory.delete\n- Disable: update the rule JSON with active=false\n- Search rules: use memory.recall with type='rule'\n\nCONFLICTS:\nIf a new rule conflicts with an existing one, tell the user:\n'You have an existing rule: [old rule]. This new one would conflict. Which should I keep?'",
    "goals": [
      "Interpret natural language teaching into structured rules",
      "Confirm every rule with the user before storing",
      "Handle all three teaching modes: instruction, correction, demonstration",
      "Manage existing rules: list, update, disable, delete",
      "Detect and resolve rule conflicts"
    ],
    "limitations": [
      "Cannot access device tools directly — only memory tools",
      "Cannot execute rules — only creates them for the orchestrator to match",
      "Must confirm every rule before storing"
    ],
    "communication_style": {
      "tone": "warm",
      "verbosity": "concise"
    }
  },
  "tools": [
    {
      "id": "tool-teach-store",
      "name": "memory.store",
      "description": "Store a new taught rule. Use type='rule' and content as structured JSON string with trigger, conditions, action, etc. Include relevant tags for matching.",
      "inputs": [
        {
          "name": "type",
          "type": "string",
          "required": true,
          "description": "Always 'rule' for taught rules"
        },
        {
          "name": "content",
          "type": "string",
          "required": true,
          "description": "Structured rule JSON: {trigger, trigger_type, conditions, action, action_type, confidence, active, created_from, description}"
        },
        {
          "name": "tags",
          "type": "array",
          "required": false,
          "description": "Tags for matching: contact names, situation types, keywords"
        },
        {
          "name": "context",
          "type": "string",
          "required": false,
          "description": "Context about when/why this rule was taught"
        }
      ],
      "output": {
        "type": "object",
        "description": "Stored rule confirmation with memory ID"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.store"
      }
    },
    {
      "id": "tool-teach-recall",
      "name": "memory.recall",
      "description": "Search existing rules and memories. Use to check for conflicts before creating a new rule.",
      "inputs": [
        {
          "name": "query",
          "type": "string",
          "required": true,
          "description": "Search query"
        },
        {
          "name": "type",
          "type": "string",
          "required": false,
          "description": "Filter: 'rule' for taught rules, or preference/fact/instruction"
        },
        {
          "name": "limit",
          "type": "number",
          "required": false,
          "description": "Max results"
        }
      ],
      "output": {
        "type": "object",
        "description": "Matching memories/rules"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.recall"
      }
    },
    {
      "id": "tool-teach-list",
      "name": "memory.list",
      "description": "List all taught rules. Use type='rule' to see all rules.",
      "inputs": [
        {
          "name": "type",
          "type": "string",
          "required": false,
          "description": "Filter by type (use 'rule' for taught rules)"
        },
        {
          "name": "limit",
          "type": "number",
          "required": false,
          "description": "Max results"
        },
        {
          "name": "offset",
          "type": "number",
          "required": false,
          "description": "Pagination offset"
        }
      ],
      "output": {
        "type": "object",
        "description": "List of rules with pagination"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.list"
      }
    },
    {
      "id": "tool-teach-update",
      "name": "memory.update",
      "description": "Update an existing rule by ID. Use to modify, enable, or disable a rule.",
      "inputs": [
        {
          "name": "id",
          "type": "number",
          "required": true,
          "description": "Rule memory ID"
        },
        {
          "name": "content",
          "type": "string",
          "required": false,
          "description": "Updated rule JSON"
        },
        {
          "name": "tags",
          "type": "array",
          "required": false,
          "description": "Updated tags"
        },
        {
          "name": "context",
          "type": "string",
          "required": false,
          "description": "Updated context"
        }
      ],
      "output": {
        "type": "object",
        "description": "Updated rule"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.update"
      }
    },
    {
      "id": "tool-teach-delete",
      "name": "memory.delete",
      "description": "Delete a taught rule by ID. Use when user says 'forget that rule' or 'remove that automation'.",
      "inputs": [
        {
          "name": "id",
          "type": "number",
          "required": true,
          "description": "Rule memory ID to delete"
        }
      ],
      "output": {
        "type": "object",
        "description": "Deletion confirmation"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.delete"
      }
    },
    {
      "id": "tool-teach-rules-match",
      "name": "memory.rules.match",
      "description": "Check for existing rules that match a situation. Use to detect conflicts before creating a new rule.",
      "inputs": [
        {
          "name": "situation",
          "type": "string",
          "required": true,
          "description": "Situation description to match against"
        },
        {
          "name": "limit",
          "type": "number",
          "required": false,
          "description": "Max results"
        }
      ],
      "output": {
        "type": "object",
        "description": "Matching rules with relevance scores"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.rules.match"
      }
    },
    {
      "id": "tool-teach-rules-count",
      "name": "memory.rules.count",
      "description": "Count total taught rules — active and inactive.",
      "inputs": [],
      "output": {
        "type": "object",
        "description": "Rule counts: total, active, inactive"
      },
      "source": {
        "type": "mcp_bridge",
        "connection_id": "memory-mcp",
        "mcp_tool": "memory.rules.count"
      }
    }
  ],
  "connectors": [
    "memory-mcp",
    "personal-assistant-ui-mcp"
  ],
  "engine": {
    "model": "claude-sonnet",
    "temperature": 0.5,
    "rv2": {
      "max_iterations": 30
    }
  },
  "grant_mappings": [],
  "access_policy": {
    "rules": [
      {
        "tools": [
          "*"
        ],
        "requires_grants": [
          "pa.verified_user"
        ],
        "effect": "allow"
      }
    ]
  },
  "intents": {
    "supported": [
      {
        "id": "i1",
        "description": "Default",
        "examples": [
          "help"
        ]
      }
    ]
  },
  "scenarios": [
    {
      "id": "s1",
      "title": "Test",
      "steps": [
        "test"
      ],
      "expected_outcome": "ok"
    }
  ]
}